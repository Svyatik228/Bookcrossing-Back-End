language: csharp

dotnet: 3.1.301
sudo: false

env:
    global:
        - DOTNET_CLI_TELEMETRY_OPTOUT: 1

script:
    - dotnet build src/BookCrossingBackEnd.sln -c Release
    - dotnet test -c Release --no-build ApplicationTest/ApplicationTest.csproj /p:CollectCoverage=true /p:CoverletOutputFormat=opencover